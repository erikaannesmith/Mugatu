<%= form_for [:designer, @rating.order, @rating] do |f| %>

  <%= f.label :efficiency %>
  <%= f.select :efficiency, [1, 2, 3, 4, 5] %>

  <%= f.label :accuracy %>
  <%= f.select :accuracy, [1, 2, 3, 4, 5] %>

  <%= f.submit "Rate Order" %>

<% end %>
